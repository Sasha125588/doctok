{
  "name": "doctok",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:web": "bun run --cwd apps/web lint:oxlint",
    "format:web": "bun run --cwd apps/web format",
    "lint-staged": "lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.8.9"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx,js,jsx,md,mdx,css,vue}": [
      "bun run lint:web",
      "bun run format:web"
    ]
  },
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*"
  ]
}
